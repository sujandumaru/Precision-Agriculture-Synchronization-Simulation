"""Discrete-event synchronization model.

Present so the module can also be run as a package from the repository root:

    python -m simulation.simulate_sync --config simulation/config.json --out simulation/results
"""
